import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.listadecompras.carla',
  appName: 'app.listadecompras',
  webDir: 'public'
};

export default config;
